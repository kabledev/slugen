export { slugen } from "./slugen";
export type { SlugenOptions } from "./types";
export { locales } from "./locales";

// usage:
// import { slugen } from 'slugen'
// slugify("İstanbul Boğazı", { locale: 'tr' }) => "istanbul-bogazi"
